[pytest]
DJANGO_SETTINGS_MODULE = forensic.settings
pythonpath = backend samples/synthetic
testpaths = tests/backend
addopts = -p no:cacheprovider -q -m "not heavy"
markers =
    heavy: multi-GB / long-running validation (run deliberately: pytest -m heavy -s)
filterwarnings =
    ignore::DeprecationWarning
