**What this changes**

**Why**

**How it was checked**

- [ ] `pytest -q` passes
- [ ] `npm run typecheck`, `npm run lint` and `npm test` pass in `frontend/`
- [ ] Both rule engines still agree (`frontend/src/rules/parity.test.ts`) when a rule or an engine changed
- [ ] Documentation under `docs/` updated when behaviour changed
- [ ] No real evidence, personal data or credentials in the diff
