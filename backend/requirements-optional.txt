# Optional capabilities. Install what you need into the same venv:
#   Windows:        .venv\Scripts\python.exe -m pip install -r backend\requirements-optional.txt
#   Linux / macOS:  .venv/bin/python -m pip install -r backend/requirements-optional.txt
#
# PST/OST mailbox support (libpff bindings). It builds from source:
#   Windows:  Visual Studio C++ build tools, or the community wheel libpff-python-windows (verify the uploader on PyPI first)
#   Linux:    apt install build-essential python3.13-dev (or the distribution's equivalent), then pip install libpff-python
libpff-python

# YARA scanning of attachments with rules dropped in backend/data/yara/*.yar
# (wheels exist up to CPython 3.13)
yara-python>=4.5.4
